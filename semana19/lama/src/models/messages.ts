export enum sucessMessage{
    createBand = "banda criada com sucesso"
};

export enum failureMessage{
    password = "senha com menos de 6 digitos",
    login = "usuario ou senha inválidos",
    admin = "usuario não tem permissão para realizar esta ação"
};